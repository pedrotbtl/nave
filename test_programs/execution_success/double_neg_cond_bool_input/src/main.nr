unconstrained fn main(a: bool) -> pub Field {
    if ((!a) > (!a)) {
        (-(-(a as Field)))
    } else {
        (-(-(a as Field)))
    }
}
unconstrained fn verify_assert(b: bool) { assert(false); }
